# Asyncy Write

Write any object to a file.

#### Example

```storyscript
# Storyscript
data = {
  "key": "value"
}
write data `/file.json`
```

`/file.json` now has `{"key":"value"}` as it's contents.
